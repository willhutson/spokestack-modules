{
  "name": "@spokestack/listening",
  "version": "1.0.0",
  "description": "Social listening module for SpokeStack",
  "main": "src/agent/listening-agent.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@spokestack/module-sdk": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  }
}
