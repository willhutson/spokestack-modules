{
  "name": "@spokestack/rfp",
  "version": "1.0.0",
  "description": "RFP module for SpokeStack — Request for proposal management with response scoring, comparison, and contract award tracking.",
  "main": "src/tools/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "validate": "node -e \"require('@spokestack/module-sdk/cli/validate').validateModule('.')\"",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@spokestack/module-sdk": "workspace:*",
    "@spokestack/core-schema": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  }
}
