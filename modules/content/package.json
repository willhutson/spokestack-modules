{
  "name": "@spokestack/content",
  "version": "1.0.0",
  "description": "LMTD's full creative pipeline — DAM, video, moodboards, presentations, docs, and content approvals",
  "main": "src/agent/content-agent.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "validate": "node -e \"require('@spokestack/module-sdk/cli/validate').validateModule('.')\"",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@spokestack/module-sdk": "workspace:*",
    "@spokestack/core-schema": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  }
}
