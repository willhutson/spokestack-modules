{
  "name": "@spokestack/press-releases",
  "version": "1.0.0",
  "description": "Press Releases module for SpokeStack — drafting, distribution, and pickup tracking",
  "main": "src/agent/press-release-agent.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "validate": "node -e \"require('@spokestack/module-sdk/cli/validate').validateModule('.')\"",
    "lint": "tsc --noEmit"
  },
  "dependencies": { "@spokestack/module-sdk": "workspace:*", "@spokestack/core-schema": "workspace:*" },
  "devDependencies": { "typescript": "^5.3.0", "vitest": "^1.2.0" }
}
