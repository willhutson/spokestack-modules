{
  "name": "@spokestack/access-control",
  "version": "1.0.0",
  "description": "Access Control module for SpokeStack — policies, rules, assignments, and permission checking",
  "main": "src/agent/access-control-agent.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "validate": "node -e \"require('@spokestack/module-sdk/cli/validate').validateModule('.')\"",
    "lint": "tsc --noEmit"
  },
  "dependencies": { "@spokestack/module-sdk": "workspace:*", "@spokestack/core-schema": "workspace:*" },
  "devDependencies": { "typescript": "^5.3.0", "vitest": "^1.2.0" }
}
