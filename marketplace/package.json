{
  "name": "@spokestack/marketplace",
  "version": "0.1.0",
  "description": "SpokeStack Module Marketplace — registry, installer, billing, and composition",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@spokestack/module-sdk": "workspace:*",
    "@spokestack/core-schema": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  }
}
